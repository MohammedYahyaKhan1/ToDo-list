# ToDo-list
This is a basic interactive To-Do style web application developed using HTML and JavaScript. The project demonstrates fundamental concepts of DOM manipulation and event handling.  The application allows users to type text into an input field, and the entered value is dynamically displayed on the webpage in real-time. 
The main goal of this project is to understand how user input can be handled and reflected on the UI without reloading the page.
